<UserControl
    x:Class="AmalgadonPlugin.Controls.OverlayButton"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    mc:Ignorable="d"
    d:DesignWidth="160" d:DesignHeight="36"
    Visibility="Collapsed">
    <UserControl.Resources>
        <!-- Amalgadon brand palette (mirrors web app CSS variables) -->
        <!-- bg-panel #221508, gold #c89b3c, gold-light #e8c060, gold-dim #b08030, bg-hover #3a2a18, text-primary #f0e6c8 -->
        <Style x:Key="AmalgadonButtonStyle" TargetType="Button">
            <Setter Property="Background"       Value="#DD221508" />
            <Setter Property="Foreground"       Value="#f0e6c8" />
            <Setter Property="BorderBrush"      Value="#c89b3c" />
            <Setter Property="BorderThickness"  Value="1" />
            <Setter Property="Padding"          Value="12,6" />
            <Setter Property="FontSize"         Value="13" />
            <Setter Property="FontWeight"       Value="SemiBold" />
            <Setter Property="Cursor"           Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Root"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background"  Value="#EE3a2a18" />
                                <Setter Property="BorderBrush" Value="#e8c060" />
                                <Setter Property="Foreground"  Value="#e8c060" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background"  Value="#FF1a1008" />
                                <Setter Property="BorderBrush" Value="#b08030" />
                                <Setter Property="Foreground"  Value="#b08030" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Button x:Name="OpenButton"
            Style="{StaticResource AmalgadonButtonStyle}"
            Content="Open in Amalgadon"
            Click="OpenButton_Click" />
</UserControl>
